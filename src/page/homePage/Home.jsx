import React, { ReactElement, FC } from "react";
import Grid from '@mui/material/Grid';


import {
  atom,
  selector,
  useRecoilState,
  useRecoilValue,
} from 'recoil';

const HomePage = ({}) => {
  return (
    <>test</>
  );
};

export default HomePage;